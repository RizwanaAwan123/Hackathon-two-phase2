/** @type {import('next').NextConfig} */
const nextConfig = {
  // appDir is now enabled by default in recent Next.js versions
  // No need for experimental flag anymore
}

module.exports = nextConfig